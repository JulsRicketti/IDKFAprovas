#{extends 'main.html' /}
#{set title:'Home' /}

<h1>Administradores do Sistema</h1>

<ul>
	#{list items:provas, as:'administrador'}
		<li>${administrador.codigo} - ${administrador.nome}</li>
	#{/list}
</ul>
	
<form method="POST" action="@{criar}">
	Nome do Administrador: <input type="text" name="nome">
	Codigo: <input type="text" name="codigo">
	<input type="Submit" value="Criar Administrador">
</form>
